import Dashboard from '@/features/dashboard/pages/Dashboard'

/**
 * DashboardPage — rota /dashboard
 * Responsabilidade: apenas montar o conteúdo da feature.
 * Toda estilização vive em features/dashboard/pages/Dashboard.tsx
 */
export default function DashboardPage() {
  return <Dashboard />
}
