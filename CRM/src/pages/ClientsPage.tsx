import ClientManagement from '@/features/clients/pages/ClientManagement'

/**
 * ClientsPage — rota /clientes
 * Responsabilidade: apenas montar o conteúdo da feature.
 * Toda estilização vive em features/clients/pages/ClientManagement.tsx
 */
export default function ClientsPage() {
  return <ClientManagement />
}
